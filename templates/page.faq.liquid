{% comment %}
  FAQ Page Template
  Converted from React FAQPage component
{% endcomment %}

<main style="flex-grow: 1;">
  <!-- Hero Section -->
  <section style="padding: 9.6rem 2.4rem 6.4rem; text-align: center;">
    <div style="max-width: 1000px; margin: 0 auto;">
      <h1 style="font-family: var(--font-heading); font-size: clamp(4rem, 6vw, 6rem); font-weight: 300; letter-spacing: 0.05em; margin-bottom: 3.2rem; color: var(--color-orea-dark);">
        {{ page.title }}
      </h1>
      <div style="width: 6.4rem; height: 1px; background-color: var(--color-orea-gold); margin: 0 auto 4.8rem;"></div>
    </div>
  </section>

  <!-- FAQ Accordion Section -->
  <section style="padding: 0 2.4rem 12.8rem;">
    <div style="max-width: 1000px; margin: 0 auto;">
      <div style="border-top: 1px solid var(--color-orea-linen);">
        {% for block in section.blocks %}
          <div class="accordion-item" style="border-bottom: 1px solid var(--color-orea-linen);">
            <button class="accordion-header" style="width: 100%; padding: 3.2rem 0; text-align: left; display: flex; justify-content: space-between; align-items: center; cursor: pointer; background: none; border: none;">
              <span style="font-size: 1.6rem; font-weight: 500; letter-spacing: 0.05em; color: var(--color-orea-dark);">
                {{ block.settings.question }}
              </span>
              <span class="accordion-icon" style="font-size: 2rem; color: var(--color-orea-taupe); transition: transform 0.3s ease;">+</span>
            </button>
            <div class="accordion-content" style="max-height: 0; overflow: hidden; transition: max-height 0.3s ease;">
              <div style="padding-bottom: 3.2rem; color: var(--color-orea-taupe); font-size: 1.5rem; line-height: 1.8;">
                {{ block.settings.answer }}
              </div>
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  </section>
</main>

<style>
  .accordion-item.active .accordion-content {
    max-height: 500px;
  }
  .accordion-item.active .accordion-icon {
    transform: rotate(45deg);
  }
</style>

{% schema %}
{
  "name": "FAQ Page",
  "settings": [],
  "blocks": [
    {
      "type": "faq_item",
      "name": "FAQ Item",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question"
        },
        {
          "type": "richtext",
          "id": "answer",
          "label": "Answer"
        }
      ]
    }
  ]
}
{% endschema %}
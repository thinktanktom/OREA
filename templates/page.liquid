{% comment %}
  Default Page Template
  Used for all pages without a specific template
{% endcomment %}

<main style="flex-grow: 1; background-color: var(--color-orea-cream);">
  <section style="padding: 12rem 2.4rem 6.4rem; text-align: center;">
    <div style="max-width: 1000px; margin: 0 auto;">
      <h1 style="font-family: var(--font-heading); font-size: clamp(4rem, 6vw, 6rem); font-weight: 300; letter-spacing: 0.05em; margin-bottom: 3.2rem; color: var(--color-orea-dark);">
        {{ page.title }}
      </h1>
      <div style="width: 6.4rem; height: 1px; background-color: var(--color-orea-gold); margin: 0 auto 4.8rem;"></div>
    </div>
  </section>

  <section style="padding: 0 2.4rem 12.8rem;">
    <div style="max-width: 1000px; margin: 0 auto;">
      <div style="font-size: 1.6rem; line-height: 1.8; color: var(--color-orea-taupe);">
        {{ page.content }}
      </div>
    </div>
  </section>
</main>
